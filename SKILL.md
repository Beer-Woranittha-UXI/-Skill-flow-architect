---
name: flow-architect
description: ตรวจ ตบ และ render Flowchart / User Flow ที่ดีไซเนอร์ร่างมาแล้ว ให้ตรงมาตรฐานทีม UXUI แล้วออกเป็นไฟล์ที่ตรวจย้อนได้ด้วย validator ตามกฎ ISO 5807. ใช้เมื่อผู้ใช้พูดว่า "ตบ flow" "รีวิว flow" "เช็ค flow" "flow ครบยัง" "flow ขาดตรงไหน" "ช่วยดู flow" "review my flow" · เมื่อส่งไฟล์ .flow ภาพผังที่วาดไว้ หรือ Figma frame ที่เป็นผังมาให้ดู · เมื่อ Decision หรือข้าวหลามตัดซ้อนกันหลายชั้นแล้วอยากรู้ว่าเงื่อนไขครบไหม · เมื่อต้องหา edge case หรือ permutation ที่ยังไม่ปิด · เมื่อต้องทำ diagram ของ user flow ส่ง dev หรือ QA. เริ่มทำงานจากเส้นทางที่ดีไซเนอร์คิดมาแล้วเท่านั้น — ถ้ายังไม่มี มันส่งแบบฟอร์มให้กรอกและตั้งคำถาม แทนการร่าง flow ให้. ครอบเฉพาะ flow ที่ผู้ใช้เดินบนหน้าจอ — data flow diagram, CI/CD workflow, cash flow, sequence diagram, architecture diagram ใช้สกิลอื่น · wireflow, prototype, mock data เป็นการ์ดแยก
license: MIT
version: 1.6.2
category: Design/UX
mcps_required: []
mcps_optional: [figma]
---

# 🧭 Flow Architect

> ดีไซเนอร์คิดเส้นทาง — สกิลนี้ตบให้ตรงสเปก ตรวจกฎ ชี้รูที่ยังไม่ปิด แล้ว render เป็น diagram
> **หนึ่งใบ = หนึ่ง flow = หนึ่งงานของผู้ใช้** จบที่ flow เท่านั้น

---

## ⛔ ข้อบังคับข้อเดียวที่สำคัญกว่าทุกข้อ

**เริ่มทำงานเมื่อมีเส้นทางของดีไซเนอร์อยู่ในมือแล้ว** — สกิลนี้ทำหน้าที่ตบ ตรวจ และชี้รู
ไม่ใช่คิด flow ตั้งต้นแทนคน

เหตุผลที่ไม่ใช่แค่สไตล์: flow คือที่ที่ดีไซเนอร์ตัดสินใจเรื่องโปรดักต์ (ผู้ใช้ควรเจออะไรก่อน
เงื่อนไขไหนสำคัญพอจะกั้นทาง ถ้าพลาดควรไปไหน) ถ้า AI คิดให้ ดีไซเนอร์จะเสีย reasoning
ที่ต้องเอาไปใช้ตอนคุยกับ PM/dev และเสียความเป็นเจ้าของงานที่ต้องปกป้องใน review
งานที่ออกมาเร็วแต่ไม่มีคนเข้าใจมันจริง ๆ คือหนี้ ไม่ใช่ผลงาน

**Gate:** ถ้าที่ได้รับมามีแค่เป้าหมายหรือชื่อฟีเจอร์ ("ทำ flow ซื้อของให้", "flow สมัครสมาชิกเป็นยังไง")
ให้ส่ง `assets/_template.flow` ให้กรอก + ถามคำถามตั้งต้น 5 ข้อ ตาม
`references/review-protocol.md` §เมื่อดีไซเนอร์ยังไม่มีอะไรเลย แล้วหยุดรอคำตอบ
การส่งแบบฟอร์มกลับไปในสถานะนี้คือผลลัพธ์ที่ถูกต้องของเทิร์นนั้น

ผ่าน gate เมื่อมีอย่างใดอย่างหนึ่ง: ไฟล์ `.flow` · ผัง/รูป/Figma frame ที่วาดไว้แล้ว ·
ข้อความที่ไล่ขั้นเองมา (เช่น "กดซื้อ → เช็คล็อกอิน → ถ้าไม่ล็อกอินไปหน้า login → ...") ·
UX blueprint ที่มี flow อยู่ข้างใน

**เกณฑ์ขั้นต่ำของ "ร่าง" — ครบ 4 ข้อถึงเริ่มตรวจ** (`self_check.py` ตรวจให้เป็น `D1`)

| # | ต้องมี | ทำไมแค่นี้ |
|---|---|---|
| 1 | จุดเริ่ม 1 จุด + จุดจบอย่างน้อย 1 จุด | บอกขอบเขต |
| 2 | step ระหว่างทาง ≥ 3 ที่ดีไซเนอร์เขียนเอง | พิสูจน์ว่ามีเส้นทางในหัวแล้ว |
| 3 | decision point ≥ 1 ที่ดีไซเนอร์ตั้งเอง | พิสูจน์ว่าเห็นทางแยกอย่างน้อยหนึ่งจุด |
| 4 | `GOAL` 1 ประโยคว่า flow นี้ให้ผู้ใช้ทำอะไรสำเร็จ | ใช้ตัดสินว่า step ไหนออกนอกเป้า |

ตั้งใจให้เบา — ต่ำกว่านี้คือยังไม่ได้คิด สูงกว่านี้คนจะเลี่ยงไม่ร่าง
**ขาดข้อไหนให้ถามให้เขาเติมเอง ห้ามเติมให้**

---

## เมื่อไหร่ที่ใช้ / ไม่ใช้

**ใช้** — ตรวจ flow ที่ร่างไว้, ตบ flow ให้ตรง template ทีม, หา edge case ที่ยังไม่ปิด,
แปลงผังที่วาดในกระดาษ/Figma เป็นไฟล์ที่ตรวจได้, ทำ diagram ส่ง dev/QA

**ไม่ใช้**
- ยังไม่มี flow ของคน → ทำตาม Gate ข้างบน (ถ้าต้องเริ่มจากศูนย์จริง ทำ blueprint ก่อน
  ด้วย `ux-strategist` แล้ววนกลับมา)
- ต่อ wireframe เข้ากับ flow → **Wireflow Builder** (การ์ดแยก)
- ทำ prototype / mock data → **Prototype & Mock-Data Engine** (การ์ดแยก)
- เขียน microcopy ในหน้าจอ → `ux-writing` / `modal-writer`
- ตรวจ DS compliance ใน Figma → `figma-audit-ui`

สกิลปลายทางข้างบนมีเท่าที่เครื่องนั้นติดตั้งไว้ — ตอนนี้ยืนยันว่ามีจริงแค่ `ux-writing`
กับ `modal-writer` ที่เหลือยังเป็นการ์ดที่ยังไม่ได้ทำ **ถ้าปลายทางไม่มี ให้บอกผู้ใช้ตรง ๆ
ว่างานนั้นอยู่นอกใบนี้และยังไม่มีสกิลรองรับ** แทนการสั่งให้ไปเรียกสกิลที่ไม่มีอยู่

---

## 🔄 ขั้นตอน

### Step 0 — Gate + เก็บบริบท

1. เช็ค Gate ข้างบน ถ้าไม่ผ่าน ส่งแบบฟอร์ม + คำถาม แล้วจบเทิร์น
2. สแกนบริบทของโปรเจกต์ (เจออันไหนใช้อันนั้น ไม่ต้องถามซ้ำ):
   `docs/blueprints/ux-*.md` · `docs/intent/*.md` · `design.md` / `tokens.json` ·
   `docs/writing/*.md` หรือ `docs/brand/voice-tone.md` · `.flow` ใบอื่นในโปรเจกต์
   (เพื่อให้ terminology และรูปทรงตรงกันข้าม flow)
3. อ่าน `references/flow-rules.md` ให้ครบก่อนวิจารณ์ — เวลารายงานต้องอ้าง code ได้ทุกข้อ

**จบขั้นนี้เมื่อ:** เขียนได้ว่า input ของดีไซเนอร์คืออะไร (ไฟล์ / ภาพ / ข้อความที่ไล่ขั้นเอง)
และลิสต์ไฟล์บริบทที่สแกนเจอออกมาได้ — หรือส่งแบบฟอร์ม + คำถามไปแล้วและจบเทิร์น

### Step 1 — ถอดของดีไซเนอร์เป็น `.flow`

เขียนสิ่งที่ดีไซเนอร์ให้มาเป็นไฟล์ `.flow` ตามที่ให้มา — syntax ดู `references/flow-dsl.md`
(ถ้าเขาส่ง `.flow` มาแล้ว ข้ามขั้นนี้)

- ตรงที่อ่านไม่ออกหรือไม่แน่ใจ ใส่ `???` ไว้ในข้อความกล่อง แล้วยกไปถามใน §5 ของรายงาน —
  ช่องว่างที่เห็นชัดมีค่ามากกว่าช่องว่างที่ถูกกลบด้วยการเดา
- ใส่เฉพาะเส้นทางที่ปรากฏในสิ่งที่เขาให้มา ส่วนที่คิดว่าน่าจะมี เก็บไปเสนอใน §4
- สรุปกลับให้เขายืนยันว่าถอดถูกก่อนไปต่อ

**จบขั้นนี้เมื่อ:** ทุกกล่องและทุกเส้นใน `.flow` ชี้กลับได้ว่ามาจากส่วนไหนของสิ่งที่ดีไซเนอร์ให้มา
และดีไซเนอร์ยืนยันว่าถอดถูก

### Step 2 — Render + ตรวจ

```bash
python3 scripts/render_flow.py <flow>.flow -o <flow>.html
python3 scripts/self_check.py <flow>.flow --meta <flow>.meta.json
```

วาด diagram ด้วย `render_flow.py` เท่านั้น — geometry ทั้งหมดออกจากสคริปต์นี้ที่เดียว
(จัด **ซ้าย→ขวา** Yes ไปต่อทางขวา No แตกลงล่าง · ทางเข้าข้าวหลามตัดรวมที่ปลายซ้าย · Yes ออกขวา No ออกล่าง คนละด้านเสมอ · radius 16 · padding 16/24 · ข้าวหลามตัดเท่ากันทุกอัน · เส้น 500px · ป้าย 100px ·
เส้นคนละรูปแบบเกาะคนละด้าน · มุมฉากมนไม่มีเส้นเฉียง) จึงวัดย้อนได้ทุกใบ
SVG ที่เขียนมือหรือ Mermaid ตรวจย้อนไม่ได้ — ตัวเลขจะหลุดสเปกโดยไม่มีใครรู้

R0 ที่แก้ได้เชิงกลไก (รูปทรงผิดประเภท, กล่องซ้ำ) แก้แล้วรายงานใน §2
R0 ที่แตะโครง flow (Decision ทางออกเดียว, ถามสองเรื่อง) ยกไปเป็นข้อเสนอใน §3

**จบขั้นนี้เมื่อ:** `self_check.py` รายงาน `R0=0` หรือ R0 ที่เหลือทุกข้ออยู่ใน §3 พร้อมทางเลือก
อย่างน้อย 2 ทาง และไฟล์ `.html` + `.meta.json` ถูกสร้างจริง

### Step 3 — รายงาน

ใช้โครงรายงาน 6 หัวข้อจาก `references/review-protocol.md` ตามลำดับนั้น
(อ่าน flow → ตบแล้ว → ผิดกฎ+ทางเลือก → ข้อเสนอที่รออนุมัติ → คำถาม → ที่มาของ flow)

หัวใจอยู่ที่ §3 กับ §4: เสนอเป็นทางเลือกพร้อมเหตุผล และติดป้ายทุกข้อว่า
**เดา** หรือ **มีหลักฐาน** จากไหน (blueprint / หน้าจอจริง / flow ใบอื่น) ดีไซเนอร์ต้องเถียงกลับได้

**จบขั้นนี้เมื่อ:** รายงานมีครบ 6 หัวข้อ · ทุกข้อใน §3 และ §4 ติดป้ายเดา/มีหลักฐาน ·
§6 มีตัวเลขสัดส่วนกล่องที่ดีไซเนอร์เขียนกับกล่องที่มาจากข้อเสนอ

### Step 3.5 — ให้ sub agent ตรวจซ้ำก่อนส่งให้ดีไซเนอร์ตัดสิน

ข้อเสนอที่ผิดกฎเสียเอง หรือ "เจอปัญหา" ที่ไม่มีจริง ทำให้ดีไซเนอร์เลิกเชื่อทั้งรายการ
สคริปต์ตรวจได้เฉพาะสิ่งที่วัดได้ — ที่เหลือต้องมีคนอ่านซ้ำ

เรียก sub agent (`Explore` หรือ `general-purpose`) พร้อมส่ง `.flow` · `.meta.json` ·
รายงานร่าง แล้วให้ตอบกลับ 4 ข้อนี้เท่านั้น

1. ข้อเสนอข้อไหน**ผิดกฎใน `flow-rules.md` เสียเอง**
2. ข้อไหน**อ้างว่าเจอปัญหาที่ไม่มีอยู่จริง**ในไฟล์
3. ข้อไหน**ติดป้ายเดา/มีหลักฐานไม่ตรง**กับสิ่งที่อ้าง
4. ข้อไหนเป็น**การคิด flow แทนคน** (เสนอเป็นคำตอบสำเร็จรูป แทนที่จะเป็นคำถาม)

ข้อที่ sub agent ทักให้ถอดออกหรือเขียนใหม่ก่อนส่ง **อย่าเถียงกลับเอง**

**จบขั้นนี้เมื่อ:** sub agent ตอบครบ 4 ข้อ และข้อที่ถูกทักถูกจัดการแล้ว

### Step 4 — วนซ้ำเมื่ออนุมัติ

รับข้อเสนอที่อนุมัติ → เขียนลง `.flow` พร้อมคอมเมนต์ `# [AI-A1] เหตุผล` ต่อท้ายบรรทัด
→ render + ตรวจใหม่ → รายงานสั้น ๆ ว่าเปลี่ยนอะไร
ข้อที่ยังไม่อนุมัติอยู่ในรายงานเฉย ๆ ไม่ขึ้น diagram และไม่ต้องทวงซ้ำ

Authorship ledger: ถ้ากล่องที่มาจากข้อเสนอ AI เกิน 40% ของทั้งใบ ให้หยุดแล้วบอกตรง ๆ
ว่าใบนี้กำลังกลายเป็น flow ของ AI แล้วเสนอให้ถอยไปคุยโครงก่อน (`self_check.py` ฟ้องเป็น `A1`)

**วนเป็นรอบ รอบละหนึ่งโฟกัส** — ยิงทุกอย่างพร้อมกันแล้วดีไซเนอร์จะปฏิเสธเหมา

| รอบ | โฟกัส | ผ่านรอบเมื่อ |
|---|---|---|
| **R1 โครงสร้าง** | `S1` `S2` `S10` `S10b` `S12` `S14` · `G3` `G6` `G7` `G8` `G9` (แก้ด้วยการจัดลำดับ branch) | ไม่มีกล่องลอย ไม่มีทางตัน เส้นไม่ทับกล่อง |
| **R2 decision** | `S3` `S4` `S5` `S6` `S6b` `S7` `S8` `S8b` `S11` `S13` | ทุกข้าวหลามตัดมี Yes/No ครบและปลายทางถูก |
| **R3 edge case** | `E1` `E2` — edge case · error state · empty state | ทุกหน้าที่ดีไซเนอร์ยอมรับว่ามีเคส ถูก `PERM` กำกับ |
| R4+ | เฉพาะที่ดีไซเนอร์ขอ | — |

`S9` `A2` `G1` `G2` อยู่นอกรอบ — เป็นข้อสังเกตที่ validator รายงานเองทุกครั้ง
ยกเข้ารายงานได้เลยโดยไม่ต้องรอรอบของมัน

**ปิดจุดที่คุยจบแล้ว** เขียน `CLOSED <id> <เหตุผล>` ลงไฟล์ `.flow`
รอบถัดไป `V1` จะบอกเองว่าเหลือจุดไหนยังไม่ปิด — ไม่ต้องพึ่งความจำของแชท

**หยุดเมื่อเจอข้อใดข้อหนึ่ง**

1. รอบล่าสุดไม่เจอ decision point ใหม่ และดีไซเนอร์ไม่รับข้อเสนอเลย
2. รอบล่าสุดเหลือแต่ข้อเสนอเรื่องจัดวาง/ชื่อกล่อง
3. ครบ R3 แล้ว — จะไป R4 ต้องเป็นดีไซเนอร์ขอ
4. ดีไซเนอร์ตอบ "เอาตามที่แนะเลย" ติดกัน 2 รอบ → **หยุดและเตือน** ว่า AI กำลังคิดแทน

**เขียน log ทุกรอบ**

```bash
python3 scripts/self_check.py <flow>.flow --log
```

ต่อท้ายตารางลง `docs/flow/log/<ชื่อไฟล์>.md` — กล่อง/เส้น · R0/R1/R2 · ปิด decision ไปกี่จุด ·
หน้าที่มีเคสพิเศษ · สัดส่วนกล่องจาก AI · รหัสที่เจอ **ไม่เขียนทับของเก่า**
เปิดแชทใหม่หรือย้ายเครื่องแล้วอ่าน log ต่อได้ทันที

**จบขั้นนี้เมื่อ:** ทุกบรรทัดที่เพิ่มมีคอมเมนต์ `[AI-xx]` กำกับ · render + ตรวจซ้ำแล้ว `R0=0` ·
และรายงานสัดส่วนที่มาใหม่แล้ว

### Step 5 — ส่งมอบ

`render_flow.py` ออกไฟล์ให้ครบในคำสั่งเดียว — ส่งต่อตามที่ปลายทางต้องการ

| ไฟล์ | ให้ใคร |
|---|---|
| `.flow` | คนแก้ต่อ — ต้นฉบับที่ใช้ตรวจย้อนได้ |
| `.html` | คนอ่าน — เปิดได้ทุกเครื่อง มีบล็อก "ที่มาของ flow" · **ย่อ/ขยาย · ลากกล่องและลากเส้นเพื่อจัดสายตา · ย้อน/ทำซ้ำ · คืนตำแหน่งเดิม** (คีย์ลัด `Ctrl/⌘ Z` · `Ctrl/⌘ ⇧ Z` · `Ctrl/⌘ +` · `Ctrl/⌘ -` · `Ctrl/⌘ 0` ขนาดจริง · `Ctrl/⌘ 9` พอดีจอ) · แถบ Tools in Flow ปิด/เปิดได้ |

**เลื่อนกล่องแล้วเส้นเดินใหม่เอง** — เลือกด้านเข้า-ออกจากตำแหน่งจริงของสองกล่องขณะนั้น ·
กล่องสี่เหลี่ยมมีจุดเกาะ 3 ช่องต่อด้าน (ข้าวหลามตัดมีช่องเดียวที่ปลายแหลม ตาม `G6`)
เส้นหลายเส้นจึงไม่กระจุกจุดเดียว · เลนที่มีเส้นอื่นจองอยู่จะถูกเลี่ยงออก 60px เอง ·
ถ้าเส้นทะลุกล่อง หน้าจะขึ้นปุ่มเตือนทันที (คลิกแล้วไฮไลต์เส้นนั้น) ไม่ต้องรอ `self_check`
(ไล่ซ้าย→ขวาเป็นหลักตาม `F-01` เยื้องแนวตั้งมากค่อยใช้บน-ล่าง) · **ดับเบิลคลิกที่เส้น**
= จัดเส้นนั้นให้สั้นและตรงที่สุด · **ดับเบิลคลิกที่กล่อง** = จัดทุกเส้นที่ต่อกับกล่องนั้น

**เส้นที่จัดเองแล้วจะไม่ถูกเดินใหม่ทับ** — ลากช่วงเส้น ลากข้อศอก หรือเพิ่ม/ลบข้อศอกเมื่อไหร่
เส้นนั้นถูกจำว่า "คนจัดเอง" · ขยับกล่องแล้วมัน **ยืดทั้งเส้นตามสัดส่วน** ปลายอีกข้างอยู่กับที่
และยังตั้งฉากล้วน · ดับเบิลคลิกที่เส้น = สั่งให้ลืมรูปที่จัดไว้แล้วเดินใหม่ให้สั้นที่สุด

**เพิ่ม/ลบข้อศอกเองได้** — ชี้ที่เส้นจะเห็นจุดจับสี่เหลี่ยมทุกข้อศอก ลากไปไหนก็ได้ (เพื่อนบ้าน
ขยับตามให้ยังตั้งฉาก) · `Alt/⌥ + คลิก` หรือ **คลิกขวา** ที่เส้น = แทรกข้อศอกใหม่ตรงจุดที่คลิก ·
ทำแบบเดียวกันที่จุดจับ = ลบข้อศอกนั้น

**ย้ายปลายเส้นได้ด้วย** — ชี้เมาส์ที่เส้นจะเห็นจุดจับสองหัว ลากไปเกาะกล่องอื่นได้อิสระ
เส้นจะเดินมุมฉากให้ใหม่เองและยังแตะขอบกล่องทั้งสองข้าง (`F-20`) · ปล่อยนอกกล่อง = เด้งกลับที่เดิม
เส้นที่ถูกย้ายปลาย **เปลี่ยนความหมายของ flow** จึงขึ้นแถบสรุปพร้อม **ปุ่มคัดลอกบรรทัด `.flow`**
และ **ปุ่มดาวน์โหลด `<ชื่อ>-edited.flow`** (ต้นฉบับถูกฝังไว้ในหน้า ป้าย `-ใช่->` `-ไม่ใช่->`
ของดีไซเนอร์ถูกคงไว้ตามเดิม) — เอาไฟล์นั้นไปแทนของเดิมแล้ว render ใหม่ ผังจริงถึงจะเปลี่ยน

**ปุ่ม 💾 บันทึก** (`Ctrl/⌘ S`) เก็บตำแหน่งและเส้นที่แก้ไว้ในเบราว์เซอร์ เปิดไฟล์ครั้งหน้าได้ของเดิมคืน
เก็บแยกต่อไฟล์ · ล้างได้ด้วยปุ่ม "ล้างที่บันทึก" · เป็นของเฉพาะเครื่องนั้น ส่งต่อให้คนอื่นไม่ได้

> **การลากใน `.html` ไม่ใช่การแก้ flow** — มันไม่เขียนกลับลง `.flow` และไม่เปลี่ยน `.meta.json`
> geometry ที่ใช้ตรวจ `G1`–`G9` ยังออกจาก `render_flow.py` ที่เดียวตาม `flow-rules.md` §3
> ใช้เพื่อดูให้ชัดตอนคุยกับทีม ถ้าอยากให้ผังเปลี่ยนจริงต้องแก้ `.flow` แล้ว render ใหม่
> (ในหน้ามีปุ่ม "คืนตำแหน่งเดิม" เสมอ)
| `.png` | **แปะ Jira / Lark / Slack ได้ทันที** — ทำอัตโนมัติบน macOS |
| `.svg` | เอาไปเปิดใน Figma / ซูมเข้าไปแล้วไม่แตก |
| `.meta.json` | เครื่องอ่าน / ใบถัดไปใช้ต่อ |

เครื่องที่ไม่ใช่ macOS จะไม่ได้ `.png` (ข้ามไปเงียบ ๆ ไม่ error) — ใช้ `.svg` แทน
หรือเปิด `.html` แล้วถ่ายหน้าจอ

**จบขั้นนี้เมื่อ:** ส่ง `.flow` + `.html` + `.meta.json` เป็นอย่างน้อย
(แนบ `.png` ด้วยถ้าปลายทางคือ Jira / Lark) และระบุสิ่งที่อยู่นอกใบนี้ (wireflow · prototype · mock data)
ว่าเป็นการ์ดแยก

---

## 📂 ไฟล์ในสกิล

| ไฟล์ | อ่านเมื่อ |
|---|---|
| `references/flow-dsl.md` | เขียน/แก้ `.flow`, ใช้ CLI |
| `references/flow-rules.md` | **อ่านก่อนวิจารณ์ทุกครั้ง** — กฎ + code R0/R1/R2 |
| `references/review-protocol.md` | ตอนรายงาน, ตอนที่ยังไม่มี flow, ตอนสงสัยว่าล้ำเส้นไหม |
| `references/style-guide.md` | token, ฟอนต์, ดึง design.md ของโปรเจกต์, เชื่อม writing skill, ส่งต่อ |
| `assets/_template.flow` | ส่งให้ดีไซเนอร์กรอกเมื่อยังไม่มี flow |
| `assets/example-buy-lottery.*` | ตัวอย่าง syntax ที่ผ่านกฎครบ (ผลงานของ Amm ใช้อ้างอิงรูปแบบ) |
| `scripts/render_flow.py` | render `.flow` → HTML + SVG + PNG + meta |
| `scripts/self_check.py` | ตรวจกฎ คืน R0/R1/R2 |
| `evals/` | จะแก้สกิลนี้ — รัน 3 สถานการณ์เทียบกับตอนไม่โหลดสกิลก่อนและหลังแก้ |

---

## ตัวอย่างการตอบที่ถูก vs ผิด

**ผู้ใช้:** "ทำ flow ถอนเงินให้หน่อย"
**ผิด:** ร่าง flow 8 กล่องให้ทันที (แม้จะบอกว่า "ปรับได้")
**ถูก:** "ใบนี้เข้ามาหลังคุณร่างเส้นทางแล้ว — ลองกรอกใน `_template.flow` แค่หัวข้อกับ
3–4 กล่องแรกก็พอ ระหว่างนี้ตอบ 5 ข้อนี้ได้ไหม: (1) ผู้ใช้กดจากหน้าไหน ... "

**ผู้ใช้:** "[ส่ง .flow มา] flow นี้ครบไหม"
**ถูก:** ถอด → render → ตรวจ → รายงาน 6 หัวข้อ โดย edge case ที่ยังไม่มี อยู่ใน §4
เป็นข้อเสนอ A1–A4 ที่ยังไม่ขึ้น diagram จนกว่าจะอนุมัติ
